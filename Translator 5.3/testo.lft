	assign 10 to x;
	assign 20 to y;
	assign y to z;
	print( +(x, 1));

	read(n);
	assign 2 to i;
	assign 1 to f;
	while (< i n){
		print(f);
		assign +(1, i) to i
	};




	read(a);
	read(b);
	while (<> a b){
		if (> a b) assign - a b to a
		else assign - b a to b 
		end
	};
	print(a)